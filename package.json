{
  "name": "pkg",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pwa export",
    "start": "sirv build -s",
    "watch": "pwa watch",
    "deploy": "npm i && pwa export && now && now alias",
    "ship": "now && now alias"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.2",
    "delay": "^4.1.0",
    "node-fetch": "^2.3.0",
    "react": "^16.5.0",
    "react-dom": "^16.5.0",
    "sirv-cli": "^0.2.0"
  },
  "devDependencies": {
    "@pwa/cli": "latest",
    "@pwa/plugin-sw-workbox": "^0.4.1",
    "@pwa/preset-react": "latest"
  },
  "browserslist": [
    ">0.25%",
    "last 1 version",
    "not ie_mob 11",
    "not ie 11",
    "not dead"
  ]
}
